{
  "file": "queries/daily_failure_rate.sql",
  "reviewed_at": "2026-05-25T16:03:13.965674",
  "issues_found": 4,
  "review": {
    "issues": [
      {
        "severity": "Critical",
        "category": "Correctness",
        "title": "Integer division",
        "line": "failed_count / total_count * 100 AS failure_rate",
        "problem": "Integer division will truncate the decimal part, leading to incorrect failure rates for values less than 100%.",
        "fix": "CAST(failed_count AS DECIMAL(10,2)) / total_count * 100 AS failure_rate"
      },
      {
        "severity": "High",
        "category": "Correctness",
        "title": "Reference alias in same SELECT clause",
        "line": "failed_count / total_count * 100 AS failure_rate",
        "problem": "Referencing an alias in the same SELECT clause can cause errors in most SQL engines.",
        "fix": "failed_count * 100.0 / total_count AS failure_rate"
      },
      {
        "severity": "Medium",
        "category": "Performance",
        "title": "Correlated subquery",
        "line": "(SELECT COUNT(*) FROM fact_transactions f2 WHERE f2.status = 'FAILED' AND f2.transaction_date = t.transaction_date) AS failed_count",
        "problem": "The correlated subquery re-scans the full table for each row in the outer query, which can be very inefficient.",
        "fix": "Use a JOIN with a pre-aggregated subquery instead of a correlated subquery."
      },
      {
        "severity": "Low",
        "category": "Readability",
        "title": "Lack of comments",
        "line": "Entire query",
        "problem": "The query lacks comments explaining the logic and calculations, making it harder to understand.",
        "fix": "Add comments to explain the purpose of each part of the query."
      }
    ],
    "corrected_sql": "WITH failed_transactions AS (\n  SELECT transaction_date, COUNT(*) AS failed_count\n  FROM fact_transactions\n  WHERE status = 'FAILED'\n  GROUP BY transaction_date\n)\nSELECT t.transaction_date,\n       COALESCE(f.failed_count, 0) AS failed_count,\n       COUNT(*) AS total_count,\n       COALESCE(f.failed_count, 0) * 100.0 / COUNT(*) AS failure_rate\nFROM fact_transactions t\nLEFT JOIN failed_transactions f ON t.transaction_date = f.transaction_date\nGROUP BY t.transaction_date\nORDER BY t.transaction_date;",
    "summary": "The query had issues with integer division, referencing aliases in the same SELECT clause, and performance due to a correlated subquery. Additionally, it lacked comments for readability."
  }
}
{
  "file": "queries/revenue_report.sql",
  "reviewed_at": "2026-05-25T16:03:32.596757",
  "issues_found": 4,
  "review": {
    "issues": [
      {
        "severity": "Critical",
        "category": "Correctness",
        "title": "Missing STATUS = 'COMPLETED' filter",
        "line": "WHERE clause",
        "problem": "The query is counting all transactions as revenue, including failed ones, which is incorrect.",
        "fix": "WHERE t.status = 'COMPLETED' AND t.transaction_date > '2024-01-01'"
      },
      {
        "severity": "Medium",
        "category": "Performance",
        "title": "Implicit JOIN syntax",
        "line": "FROM clause",
        "problem": "Using implicit JOIN syntax can lead to confusion and potential errors. It's better to use explicit JOIN syntax.",
        "fix": "FROM fact_transactions t JOIN dim_merchant m ON t.merchant_id = m.merchant_id"
      },
      {
        "severity": "Medium",
        "category": "Correctness",
        "title": "ORDER BY ascending",
        "line": "ORDER BY clause",
        "problem": "The query orders by total_revenue in ascending order, which is incorrect for a 'top 10' use case. It should be descending.",
        "fix": "ORDER BY total_revenue DESC"
      },
      {
        "severity": "Low",
        "category": "Readability",
        "title": "Lack of comments on complex logic",
        "line": "Entire query",
        "problem": "The query lacks comments explaining the logic, which can make it harder to understand for other developers.",
        "fix": "Add comments explaining the purpose of each part of the query."
      }
    ],
    "corrected_sql": "-- Revenue report by merchant\nSELECT m.merchant_name,\n       SUM(t.amount) as total_revenue,\n       COUNT(*) as txn_count\nFROM fact_transactions t\nJOIN dim_merchant m ON t.merchant_id = m.merchant_id\nWHERE t.status = 'COMPLETED' AND t.transaction_date > '2024-01-01'\nGROUP BY m.merchant_name\nORDER BY total_revenue DESC\nLIMIT 10;",
    "summary": "The query had issues with correctness and performance, and lacked readability. The corrected query now properly filters for completed transactions, uses explicit JOIN syntax, orders by descending revenue, and includes comments."
  }
}